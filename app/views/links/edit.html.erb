<h1> Edit </h1>

<%= render partial: "form", locals: {link: @link} %>

<div class="mt-4 flex gap-1 items-center">
  <%= link_to "Cancel", @link %>
  or
  <%= button_to "Delete", @link, method: :delete, date: {turbo_confirm: "Sur ?"} %>
</div>